name: Secrets Lab

on:
  workflow_dispatch:

jobs:
  test-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Use secret safely
        env:
          LAB_SECRET: ${{ secrets.LAB_TEST_SECRET }}
        run: |
          echo "Secret is available to the workflow"
          echo "Secret length: ${#LAB_SECRET}"
